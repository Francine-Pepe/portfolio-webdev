How to upddate this React App on Github:

* Stage all:
git add .

* ..and commit:
git commit -m "update build for gh-pages"

* Last step - Push to GitHub:
git push -u origin master

* npm run deploy


